{{- if eq .chezmoi.os "darwin" }}
if [ -x /opt/homebrew/bin/brew ]; then
    eval "$(/opt/homebrew/bin/brew shellenv)"
fi
{{ end -}}

export PATH="$HOME/bin:$HOME/.local/bin:$HOME/go/bin:$HOME/.lmstudio/bin:$PATH"

[[ -f ~/.bashrc ]] && source ~/.bashrc
